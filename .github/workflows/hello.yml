name: Hello
run-name: ${{ github.actor }} pressed button
on: [push]
jobs:
  Hello-World:
  runs-on: ubuntu-latest
  steps:
    - run: ${{ github.event_name }} / ${{ runner.os }} / ${{ github.ref }} / ${{ github.repository }}
    - uses: actions/checkout@v3
    - run: echo "${{ github.workspace }}" ; ls -Rla "${{ github.workspace }}"
    - run: echo "${{ job.status }}"